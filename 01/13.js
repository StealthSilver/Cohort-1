// set timeout

function greet() {
  console.log("hello everyone");
}

setTimeout(greet, 2 * 1000);
//In this code, the callback function is greet because it is passed to setTimeout to be executed after a 2-second delay.
