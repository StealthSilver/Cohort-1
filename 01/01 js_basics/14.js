// set interval

function greet() {
  console.log("hello everyone");
}

setInterval(greet, 2 * 1000);
//In this code, the callback function is greet because it is passed to setInterval to be executed after a 2-second delay.
